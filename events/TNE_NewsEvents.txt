add_namespace = TNE_NewsEvents

news_event = {
	id = TNE_NewsEvents.1
	title = TNE_NewsEvents.1.t
	desc = {
		text = TNE_NewsEvents.1.d
	}
	picture = GFX_news_event_cold_war_end
	
	major = yes
	
	trigger = {
		date > 1992.1.29
		NOT = { has_global_flag = cold_war_ended }
		country_exists = SOV
		country_exists = USA
		SOV = { has_war = no }
		USA = { has_war = no }
	}
	
	mean_time_to_happen = {
		days = 3
	}
	
	immediate = {
		set_global_flag = cold_war_ended 
	}
	
	option = {
		name = TNE_NewsEvents.1.a
	}
}